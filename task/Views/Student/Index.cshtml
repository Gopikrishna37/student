
@model IEnumerable<task.Models.Student>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Add", "Add", new { })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StudentId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StudentName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            <label>Actions</label>
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StudentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>

                @Html.ActionLink("Edit", "Edit", new { id = item.StudentId }, new { @class = "btn btn-primary" }) |
                <input type="submit" id="btnDelete" value="Delete" class="btn btn-danger" onclick="Delete('@Url.Action("Delete","Student",new {id=item.StudentId})')" />
                @*<input type="button"  id="btnDelete"  value="Delete" class="btn btn-danger" />*@
                @*             @Html.ActionLink("Delete", "Delete", new { id = item.StudentId }, new { @class = "btn btn-primary" })
                *@
            </td>
        </tr>
    }

</table>

@*<script>
        $(document).ready(function(){
            $("#btnDelete").on("click", function () {
            if (confirm("Do you want to delete this Customer?"))
            var id = $("#txtStudentId").val();
            $.ajax({
            type: "POST",
            url: '@Url.Action("Delete", "Student")',
            data: { id : id },
            success: function () {
                alert("Deleted Student ");

            },
            error: function(){
                alert("Error");
            }
            });

            });
        })
</script>*@
